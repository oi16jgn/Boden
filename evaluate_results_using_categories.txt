# psuedo
entries = all applicants in dataset and their best matches according to similarity measure
all_companies = all companies in dataset

total_amount = get_total_matches(entries, all_companies)

amount = 0

for entry in df
    for company in entry['top_matches']
        if check_match(entry, company)
            amount++
        else
            break

score = amount / total_matches

print(score)

def get_total_matches(entries, all_companies)
    amount = 0
    for entry in entries
        for company in all_companies
            if check_match(entry, company)
                amount++
    return amount

def check_match(entry, company)
    for title in entry['titles'] 
        if title is in company['titles']
            return true
    return false